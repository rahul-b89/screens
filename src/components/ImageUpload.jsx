import React from 'react'

function ImageUpload({
    titile,
    name,
    type,
    className,
    errors,
    register,
    ...props
}) {
  return (
    <div>ImageUpload</div>
  )
}

export default ImageUpload